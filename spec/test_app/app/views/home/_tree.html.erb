<%= render  partial: 'closure_tree_helper/tree_list',
            locals: {
              nodes: @tags,
              opts: {
                active_class: 'active',
                active_id: params[:id].to_i,
                active_node_key: :id,
                link_class: 'link'
              }
            }
%>
